<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.splashactivitydemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Request">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".BookARide2" />
        <activity android:name=".bookResults" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".FragmentOne" />
        <activity android:name=".FragmentTwo" />
        <activity android:name=".Status" />
        <activity android:name=".PlanBookBtn" />
        <activity android:name=".singleRow2"></activity>
    </application>

</manifest>